# Cloudflare Environment Variables Configuration
# This file contains all environment variables needed for Cloudflare Workers & Pages deployment
#
# IMPORTANT: Do NOT commit this file with real secrets to version control!
# Set these as Cloudflare secrets using: wrangler secret put VARIABLE_NAME

[env.development]
vars = { 
  ENVIRONMENT = "development",
  NODE_ENV = "development",
  VITE_APP_URL = "http://localhost:5173",
  VITE_API_URL = "http://localhost:3000",
  DEBUG_LOGGING = "true"
}

[env.staging]
vars = {
  ENVIRONMENT = "staging",
  NODE_ENV = "production",
  VITE_APP_URL = "https://staging.xflexwithai.com",
  VITE_API_URL = "https://api-staging.xflexwithai.com",
  DEBUG_LOGGING = "true"
}

[env.production]
vars = {
  ENVIRONMENT = "production",
  NODE_ENV = "production",
  VITE_APP_URL = "https://xflexwithai.com",
  VITE_API_URL = "https://api.xflexwithai.com",
  DEBUG_LOGGING = "false"
}

# Secret variables (set via: wrangler secret put VAR_NAME)
# 
# Required secrets - set these in Cloudflare Dashboard > Workers > xflexwithai > Settings > Secrets:
#
# Authentication & JWT
# - JWT_SECRET: Secret key for JWT token signing (generate with: openssl rand -base64 32)
# 
# OAuth Configuration (from Manus)
# - OAUTH_SERVER_URL: https://your-oauth-server
# - VITE_OAUTH_PORTAL_URL: https://your-oauth-portal
# - VITE_APP_ID: Your application ID
# - OWNER_OPEN_ID: Owner's OpenID
# - OWNER_NAME: Owner's name
# - VITE_APP_TITLE: XFlex Trading Academy
# - VITE_APP_LOGO: /logo.png
#
# Forge API (if using external API)
# - BUILT_IN_FORGE_API_URL: https://forge-api.example.com
# - BUILT_IN_FORGE_API_KEY: Your Forge API key
# - VITE_FRONTEND_FORGE_API_URL: https://forge-api.example.com
# - VITE_FRONTEND_FORGE_API_KEY: Your Forge API key
#
# Optional Analytics
# - VITE_ANALYTICS_ENDPOINT: Analytics service endpoint
# - VITE_ANALYTICS_WEBSITE_ID: Website ID for analytics
#
# Database (auto-bound via D1 in wrangler.toml, but you can use these for connection strings)
# - DATABASE_URL: Connection string (optional, D1 is auto-bound as ENV.DB)
#
# AWS S3 (if not using R2)
# - AWS_ACCESS_KEY_ID: Your AWS access key
# - AWS_SECRET_ACCESS_KEY: Your AWS secret key
# - AWS_REGION: AWS region (e.g., us-east-1)
#
# Cloudflare R2 (auto-bound via wrangler.toml, but you can use these for direct access)
# - CLOUDFLARE_ACCOUNT_ID: Your Cloudflare account ID
# - CLOUDFLARE_API_TOKEN: Your Cloudflare API token
# - R2_BUCKET_NAME: xflexwithai-videos
# - R2_PUBLIC_URL: https://xflexwithai-videos.YOUR_ACCOUNT.r2.dev
#
# Example command to set a secret:
# wrangler secret put JWT_SECRET --env production
# (It will prompt you to paste the value)
